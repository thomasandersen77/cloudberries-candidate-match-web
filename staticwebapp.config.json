{
  "routes": [
    {
      "route": "/api/actuator/{*rest}",
      "rewrite": "/actuator/{rest}",
      "allowedRoles": ["anonymous"]
    },
    {
      "route": "/api/{*rest}",
      "allowedRoles": ["anonymous"]
    }
  ]
}

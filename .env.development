# The base URL for your local backend API (dev via Vite proxy)
# Use proxy-first dev: forward /api and /actuator to localhost:8080 in vite.config.ts
# Leave as /api (or unset) so services call same-origin and get proxied.
VITE_API_BASE_URL=/

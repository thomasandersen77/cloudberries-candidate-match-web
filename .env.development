# Local dev: browser calls /api/* → Vite proxy forwards to http://localhost:8080/api/*
# (matches spring.mvc.servlet.path=/api). Omit env to fall back to http://localhost:8080 in apiBase.ts.
VITE_API_BASE_URL=/api

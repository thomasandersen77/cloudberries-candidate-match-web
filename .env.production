# Same-origin /api/* — Azure Static Web Apps proxies the full path to the linked Container App (portal APIs link).
# No external rewrite in staticwebapp.config.json; avoid baking the Container App URL into the client bundle.
VITE_API_BASE_URL=/api
